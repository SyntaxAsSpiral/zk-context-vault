# Staging (Generated)

This folder is generated by `python workshop/src/assemble.py` and is the input to `python workshop/src/sync.py`.

- Contents are intentionally not tracked in git.
- This stub file is tracked so the folder exists in the repo.

