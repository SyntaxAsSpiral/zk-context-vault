---
id: kiro-dotfile-vis
source: agents/kiro-hooks.md
---

👁️ Dotfile Visibility - No bare `ls` when orienting to directories. Dotfiles are organizational, not hidden. Use `ls -a` or `ls -A` for directory reconnaissance. Include `.*` patterns in glob searches. Treat dotfolders as first-class citizens.

